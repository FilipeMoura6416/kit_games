python server.py tttm .\advsearch\humanplayer\agent.py .\advsearch\your_agent\tttm_minimax.py -d 600 -p 1
python server.py tttm .\advsearch\your_agent\tttm_minimax.py .\advsearch\humanplayer\agent.py -d 600 -p 1
python server.py othello .\advsearch\your_agent\othello_minimax_count.py .\advsearch\your_agent\othello_minimax_mask.py -d 60 -p 1
python server.py othello .\advsearch\your_agent\mcts.py .\advsearch\your_agent\othello_minimax_mask.py -d 60 -p 1

advsearch\your_agent\othello_minimax_count.py
advsearch\your_agent\othello_minimax_custom.py
advsearch\your_agent\othello_minimax_mask.py
advsearch\your_agent\minimax.py
advsearch\your_agent\mcts.py
advsearch\humanplayer\agent.py

Classe que servirá de nodos para a árvore 

Class Nodo 

Cada nodo será um game state
O nodo deve guardar:
a quantidade de playouts e 
os resultados dos playout em relação ao agente
O valor do nodo segundo a fórmula

O nodo deve ter:
Um método para recalcular seu valor conforme a atualização dos playouts

Loop:
Se não estiver totalmente expandido e não for terminal
    Expandir
    Simular
Senão se estiver totalmente expandido 
    Seleciona o melhor filho, go to Loop
Senão se for terminal 

enquanto estiver totalmente expandido e não for terminal
    Seleciona o melhor filho


Negamax com iterative deepening
Criar uma classe nodo_state para evitar retrabalho na criação dos estados

Minimax iterativo:
    Aspectos:
        Ordem de avaliação:
        Moemento de ordenamento

    Classe minimax
    ideia geral:
        Ir diretamente para a última camada expandida. A classe minimax terá salvo qual o primeiro nodo da última camada, chamemos de Nu. O valore de Nu será definido pelo max de seus filhos. Essa ordenação servirá para que na próxima iteração a ordem de análise já esteja correta. Uma vez que todos os nodos da última camada tenham sido analisados, todos os nodos terão seus filhos ordenados
    Problema: Ordenar os filhos de todos os nodos pode resultar em ordenação de nodos que serão podados

    ideia geral:
        Ir diretamente para a última camada expandida. A classe minimax terá salvo qual o primeiro nodo da última camada, chamemos de Nu. O valore de Nu será definido pelo max de seus filhos. Essa ordenação servirá para que na próxima iteração a ordem de análise já esteja correta. 
    
    Ideia geral:
        Nodo inicializado com a raiz
        while true:
            Enquanto nodo não está na profundidade máxima -1 ou não é estado terminal
                Se este nodo já teve seus filhos ordenados nessa iteração
                    Ordenar os filhos do nodo
                Se ainda há filhos não verificados
                    O nodo se torna o filho mais promissor ainda não verificado
                Se não:
                    Se Nodo tem pai:
                        O nodo se torna seu pai
                    Se não:
                        Acabou     
            Avaliar todos os filhos ->> ERRO: resultará na avaliação de estados que seriam podados
            Definir o valor do nodo com o melhor dos filhos
            Nodo se torna seu pai

    Ideia geral:
        Nodo inicializado com a raiz
        while true:
            Se o nodo for terminal: --> PROBLEMA: A maior parte das vezes este teste será falso e testá-lo é custoso pois usa a função len
                Verifica o valor
                O nodo se torna se pai
            Senão, se nodo não está na profundidade máxima 
                Se este nodo já teve seus filhos ordenados nessa iteração
                    Ordenar os filhos do nodo
                Se ainda há filhos não verificados
                    O nodo se torna o filho mais promissor ainda não verificado
                Se não:
                    Se Nodo tem pai:
                        O nodo se torna seu pai
                    Se não:
                        Acabou
            Se não:
                Usa função de avaliação
                Nodo se torna seu pai

    PROBLEMA: Na maior parte das vezes o teste de se um estado é terminal será falso
    Solução: Testar uma primeira vez e guardar o resultado em uma variável. O teste passa a ser if terminal and terminal(), a variável terminal deve ser inicializada em true 
    
    Ideia geral:
        Constroi Nodo inicializado com a raiz

        while true:
            Se o nodo for terminal: 
                Verifica o valor
                O nodo se torna se pai
            Senão, se nodo não está na profundidade máxima 
                Se este nodo já teve seus filhos ordenados nessa iteração
                    Ordenar os filhos do nodo, ordenar os movimentos pois nem todos os filhos serão efetivamente criados
                Se ainda há filhos não verificados
                    O nodo se torna o filho mais promissor ainda não verificado
                Se não:
                    Se Nodo tem pai:
                        O nodo se torna seu pai
                    Se não:
                        Acabou
            Se não:
                Usa função de avaliação
                Nodo se torna seu pai
    OBS: Contruir os nodos apenas quando estes forem ser analisados e nesse momento definir se é estado terminal ou não
    OBS: Para evitar o problema de construir nodos que serão podados, usar fast eval do movimento
    Como associar os movimentos na lista de movimentos aos seus respectivos valores e aos objetos filhos?
        Cada movimento é uma chave em um dicionário que leva para uma lista contendo o valor e o objeto filho do movimento 
        Problema: Quanto o valor do nodo for atualizado terei que atualizar o valor na lista 
                    

Questão: Funcionaria avaliar toda a última camada e só após isso atualizar valores
Resposta: Não, Isso resultará na avaliação de estados que seriam podados

    

Erros:
07/07 17:07 - Erro na lógica da TT: está guardando valor de alpha como valor do estado. Isso está errado o valor de alpha não é necesariamente o valor do estado uma vez que o alpha pode ter vindo de outro nodo. ALterei para guardar o valor do filho mais promissor ou seja max(move_value[:])





function AlphaBetaWithMemory(n : node_type; alpha , beta , d : integer) : integer;

    if retrieve(n) == OK then /* Transposition table lookup */
        if n.lowerbound >= beta then 
            return n.lowerbound;
        if n.upperbound <= alpha then
            return n.upperbound;
        alpha := max(alpha, n.lowerbound);
        beta := min(beta, n.upperbound);
    if d == 0 then 
        g := evaluate(n); /* leaf node */
    else if n == MAXNODE then
        g := -INFINITY; 
        a := alpha; /* save original alpha value */
        c := firstchild(n);
        while (g < beta) and (c != NOCHILD) do
            g := max(g, AlphaBetaWithMemory(c, a, beta, d - 1));
            a := max(a, g);
            c := nextbrother(c);
    else /* n is a MINNODE */
        g := +INFINITY; 
        b := beta; /* save original beta value */
        c := firstchild(n);
        while (g > alpha) and (c != NOCHILD) do
            g := min(g, AlphaBetaWithMemory(c, alpha, b, d - 1));
            b := min(b, g);
            c := nextbrother(c);

    /* Traditional transposition table storing of bounds */

    /* Fail low result implies an upper bound */
    if g <= alpha then 
        n.upperbound := g;
        store n.upperbound;
    
    /* Fail high result implies a lower bound*/
    if g >= beta then 
        n.lowerbound := g; 
        store n.lowerbound;
    return g;

function MTDF(root : node_type; f : integer; d: integer) : integer;
    g := f;
    upperbound := +INFINITY;
    lowerbound := -INFINITY;
    repeat
        if g == lowerbound then 
            beta := g + 1 
        else
            beta := g;
        g := AlphaBetaWithMemory(root, beta - 1, beta,d);
        if g < beta then 
            upperbound := g 
        else 
            lowerbound := g;
    until lowerbound >= upperbound;
    return g;


Acesso aos filhos de um nodo
Obs: Vou deixar o move ordering de lado por enquanto
Objetivo: Evitar desperdício de tempo criando nodos já criados anteriormente e criando nodos que não serão analisados
Opção 1: Criar todos os filhos na criação do Nodo
Problema: Se o nodo estiver na última camada a ser visitada estarei criando nodos que não serão visitados e não há como saber qual a última camada que será visitada
Opção 2: Criar todos os filhos quando chegar no for
Problema: Possivelmente criando nodos que serão podados
Opção 3: 
Quando chegar no for verificar se a lista de movimentos possíveis já foi criada
    Se não criar lista de moviemntos possíveis e salvar
Para cada movimento
    Usar o moviemento como chave de acesso para um dicionário, o valor retornado será o nodo filho
    Se o nodo filho não existir ainda, criar

alpha = beta - 1
if g <= alpha
if g <= beta - 1 < beta
if g < beta 
if -g > - beta
alpha := - (beta - 1)
alpha = -beta + 1
alpha > beta
 
Minnode alpha = beta - 1:
        g := +INFINITY; 
        b := beta; /* save original beta value */
        c := firstchild(n);
        while (g > beta - 1) and (c != NOCHILD) do
            g := min(g, AlphaBetaWithMemory(c, alpha, b, d - 1));
            b := min(b, g);
            c := nextbrother(c);

        g := +INFINITY; 
        b := beta; /* save original beta value */
        c := firstchild(n);
        while (g >= beta) and (c != NOCHILD) do
            g := min(g, AlphaBetaWithMemory(c, b - 1, b, d - 1));
            b := min(b, g);
            c := nextbrother(c);

        g := -INFINITY; 
        b := - beta; /* save original beta value */
        c := firstchild(n);
        while (g < - beta) and (c != NOCHILD) do
            g := max(g, - AlphaBetaWithMemory(c, b, d - 1));
            b := max(b, g);
            c := nextbrother(c);

        beta = - 2
        recurs = 10
        recurs = - 5

To do:
    Para a seleção de próximo estado:
        Avaliação rasa usando diretamente a função de avaliação para seleção do top 3
        Alterar MTD(f) para funcionar com profundidade máxima 
        Implementar softmax
        Implementar nova feature simples de 4_edge